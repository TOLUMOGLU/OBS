@using OgrenciBilgiSistemi.Models
@model List<Sinav>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Diğer head etiketleri burada yer alabilir -->

    <title>Öğrenci Bilgi Sistemi</title>

    <!-- DataTables CSS ve JS -->
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.22/css/jquery.dataTables.css">
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.22/js/jquery.dataTables.js"></script>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <!-- DataTables CSS ve JS -->
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.22/css/jquery.dataTables.css">
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.22/js/jquery.dataTables.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <!-- Clipboard.js kütüphanesi -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/clipboard.js/2.0.8/clipboard.min.js"></script>

    <!-- SheetJS kütüphanesi -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.17.4/xlsx.full.min.js"></script>

    <!-- jsPDF kütüphanesi -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.4.0/jspdf.umd.min.js"></script>
    <script src="path-to-your-scripts-folder/FileSaver.min.js"></script>

    <!-- Diğer script etiketleri burada yer alabilir -->
</head>
<body>
    <h1>Öğretim Görevlisi Sınav Tanımlama</h1>
    <br />
    <button id="copyButton" class="btn btn-primary">Kopyala</button>
    <button id="exportExcelButton" class="btn btn-success">Excele Aktar</button>
    <button id="exportPdfButton" class="btn btn-info">PDF'e Aktar</button>

    <br /><br />

    <table class="table table-bordered">
        <tr>

            @if (User.IsInRole("Öğretim Elemanı"))
            {


                <th>#</th>
                <th>Ders Kodu</th>
                <th>Ders Adı</th>
                <th>Sınav Türü</th>
                <th>Sınav Günü</th>
                <th>Sınav Saati</th>
                <th>Derslik Adı</th>
                <th>Gözetmen Adı</th>
                <th>İşlemler</th>
            }

        </tr>
        @foreach (var item in Model)
        {
            <tr>
                @if (User.IsInRole("Öğretim Elemanı"))
                {


                    <td>@item.SinavID</td>
                    <td>@item.DersAcma.Mufredat.DersHavuzu.DersKodu</td>
                    <td>@item.DersAcma.Mufredat.DersHavuzu.DersAdi</td>
                    <td>@item.SinavTuru</td>
                    <td>@item.SinavTarihi</td>
                    <td>@item.SinavSaati</td>
                    <td>@item.Derslik.DerslikAdi</td>
                    <td>@item.OgretimElemani.Adi</td>
                    <td>
                        <a href="@Url.Action("SinavEkle", "SinavTanimla")" class="btn btn-primary">Sınav Ekle</a>

                        <button class="btn btn-success" data-toggle="modal" data-target="#duzenleModal-@item.SinavID">Düzenle</button>
                        <div class="modal fade" id="duzenleModal-@item.SinavID" tabindex="-1" role="dialog" aria-labelledby="duzenleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        @*<h5 class="modal-title" id="duzenleModal">Düzenle Öğrenci Bilgileri - @item.Adi @item.Soyadi</h5>*@
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="container">
                                        <div class="container">
                                            @using (Html.BeginForm("DuzenleOgretimElemani", "SinavTanimla", FormMethod.Post))
                                            {
                                                <div class="container">
                                                    <div class="row">
                                                        <div class="col-md-6">
                                                            <div class="form-group">
                                                                <label for="Ad">Ders Kodu:</label>
                                                                <input type="text" id="DersKodu" name="Adi" class="form-control" value="@item.DersAcma.Mufredat.DersHavuzu.DersKodu" />
                                                            </div>

                                                            <div class="form-group">
                                                                <label for="Soyadi">Ders Adı:</label>
                                                                <input type="text" id="DersAdi" name="Soyadi" class="form-control" value="@item.DersAcma.Mufredat.DersHavuzu.DersAdi " />
                                                            </div>

                                                            <div class="form-group">
                                                                <label for="OgrenciNo"> Sınav Türü :</label>
                                                                <input type="text" id="SinavTuru" name="OgrenciNo" class="form-control" value="@item.SinavTuru " />
                                                            </div>

                                                            <div class="form-group">
                                                                <label for="Durumu">Sınav Tarihi :</label>
                                                                <input type="text" id="SinavTarihi" name="Durumu" class="form-control" value="@item.SinavTarihi " />
                                                            </div>

                                                            <div class="form-group">
                                                                <label for="BolumAdi">Sınav Saati :</label>
                                                                <input type="text" id="SinavSaati" name="BolumAdi" class="form-control" value="@item.SinavSaati " />
                                                            </div>
                                                            <div class="form-group">
                                                                <label for="BolumAdi">Derslik Adı :</label>
                                                                <input type="text" id="DerslikAdi" name="BolumAdi" class="form-control" value="@item.Derslik.DerslikAdi " />
                                                            </div>
                                                            <div class="form-group">
                                                                <label for="BolumAdi">Öğretim Elmanı Adı :</label>
                                                                <input type="text" id="Adi" name="BolumAdi" class="form-control" value="@item.OgretimElemani.Adi " />
                                                            </div>

                                                            <div class="container">
                                                                <div class="row">
                                                                    <div class="col-md-2">

                                                                        <button type="submit" class="btn btn-success">Güncelle</button>


                                                                    </div>


                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>}
                                        </div>
                                        <div class="container">
                                            <div class="container">
                                                <div class="col-md-12">
                                                    @using (Html.BeginForm("IndexOgretimElemani", "SinavTanimla", FormMethod.Post))
                                                    {
                                                        <button type="submit" class="btn btn-primary">Kapat</button><br />
                                                    }


                                                </div>
                                                <br />
                                            </div>

                                        </div>

                                    </div>
                                </div>
                            </div>
                        </div>

                        @Html.ActionLink("Sil", "SilOgretimElemaniSinav", new { id = item.SinavID }, new { @class = "btn btn-danger", onclick = "return confirm('Silmek istiyor musunuz?')" })
                    </td>

                }

            </tr>


            //CRUD İŞLEMLERİNDE KALDIM KULLANICI ADINA BASTIĞINDA BİLGİLER GELMESİ GEREKİYOR
            //Bu bilgile düzenlenmeli vs vs

        }


    </table>



</body>
</html>



<!-- JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.17.5/xlsx.full.min.js"></script>
<script>

    $(document).ready(function () {
        // DataTables initialization
        var dataTable = $('#myTable').DataTable({
            "lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "Tümü"]],
            "pageLength": 10,
        });



        // Copy button
        var copy = new ClipboardJS('#copyButton', {
            target: function (trigger) {
                return document.querySelector('.table');
            }
        });


        // Copy button success message
        copy.on('success', function (e) {
            alert('Tablo kopyalandı!');
        });


    });


    // PDF KISMINA AİTTİR

    document.getElementById('convertButton').addEventListener('click', function () {
        // Tabloyu içeren elementin ID'si ile tabloyu seç
        var myTable = document.getElementById('myTable');
        var secTable = myTable.cloneNode(true);
        // Belirli bir sütunu (kolonu) silen fonksiyon
        function deleteColumn(columnIndex) {
            for (var i = 0; i < secTable.rows.length; i++) {
                secTable.rows[i].deleteCell(columnIndex);
            }
        }

        // Silinecek sütunun indeksini belirt
        var columnIndexToDelete = 6; // Örneğin, 1. sütunu sil

        // Belirtilen sütunu sil
        deleteColumn(columnIndexToDelete);


        var inputHtml = document.getElementsByTagName('head')[0].innerHTML + secTable.outerHTML;
        var dataToSend = { inputData: inputHtml };

        fetch('/AnaSayfa/ConvertToPdf', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'RequestVerificationToken': document.getElementsByName('__RequestVerificationToken')[0]
            },
            body: JSON.stringify(dataToSend)
        })
            .then(response => response.blob())
            .then(blob => {
                // Code to download the PDF file
                const url = window.URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.style.display = 'none';
                a.href = url;
                a.download = 'converted.pdf';
                document.body.appendChild(a);
                a.click();
                window.URL.revokeObjectURL(url);
            })
            .catch(error => console.error('Error:', error));
    });
    // PDF KISMINA AİTTİR


    //EXCEL E AİT
    document.getElementById('exportExcelButton').addEventListener('click', function () {
        // Tabloyu içeren elementin ID'si ile tabloyu seç
        var myTable = document.getElementById('myTable');
        var secTable = myTable.cloneNode(true);
        // Belirli bir sütunu (kolonu) silen fonksiyon
        function deleteColumn(columnIndex) {
            for (var i = 0; i < secTable.rows.length; i++) {
                secTable.rows[i].deleteCell(columnIndex);
            }
        }

        // Silinecek sütunun indeksini belirt
        var columnIndexToDelete = 6; // Örneğin, 1. sütunu sil

        // Belirtilen sütunu sil
        deleteColumn(columnIndexToDelete);

        // Export the modified table to Excel
        var wb = XLSX.utils.table_to_book(secTable, { sheet: "Sheet1" });
        var wbout = XLSX.write(wb, { bookType: 'xlsx', bookSST: true, type: 'blob' });

        // Code to download the Excel file
        saveAs(wbout, 'table.xlsx');
    });
    function saveAs(blob, fileName) {
        var a = document.createElement("a");
        document.body.appendChild(a);
        a.style = "display: none";
        var url = window.URL.createObjectURL(blob);
        a.href = url;
        a.download = fileName;
        a.click();
        window.URL.revokeObjectURL(url);
    }



</script>






















